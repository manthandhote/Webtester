{
  "name": "webrecorder",
  "version": "1.0.0",
  "description": "Record browser actions and compile them into Playwright/Selenium test cases",
  "main": "src/cli.js",
  "bin": {
    "webrecorder": "src/cli.js"
  },
  "scripts": {
    "test": "npm run test:unit && npm run test:browser",
    "test:unit": "node test/generators.test.js",
    "test:browser": "node test/injected.test.js && node test/recorder.test.js && node test/downloads.test.js && node test/replay.test.js"
  },
  "license": "MIT",
  "dependencies": {
    "exceljs": "^4.4.0",
    "playwright": "^1.47.0"
  }
}
